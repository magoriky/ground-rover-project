cmake_minimum_required(VERSION 3.0.2)
project(adl200_msgs)


find_package(catkin REQUIRED COMPONENTS
message_generation
std_msgs
)

add_message_files(
FILES
RobotMotor.msg
gps_data.msg

)

generate_messages(
DEPENDENCIES
std_msgs
)

catkin_package(
CATKIN_DEPENDS
std_msgs

)
